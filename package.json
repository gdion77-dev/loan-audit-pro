{
  "name": "loan-audit-pro",
  "version": "0.1.0",
  "private": true,
  "description": "Loan Audit PRO — Τεχνικός Έλεγχος Κανονικών Δανείων. Step 1-A: domain model & types only. Independent project: NOT related to N.3869/2010, NOT related to AP 6/2026.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build:web": "vite build",
    "lint": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts tests/*.test.tsx",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "tsx": "^4.21.0",
    "@types/node": "^25.6.0",
    "vite": "^7.0.0",
    "@vitejs/plugin-react": "^5.0.0"
  }
}
